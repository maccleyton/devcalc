// utils/multiply.ts
/**
 * Multiplica dois números
 * @param a - primeiro número
 * @param b - segundo número
 * @returns resultado da multiplicação a * b
 */
export function multiply(a: number, b: number): number {
  return a * b;
}