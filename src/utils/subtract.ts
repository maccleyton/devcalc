// utils/subtract.ts
/**
 * Subtrai dois números
 * @param a - primeiro número
 * @param b - segundo número
 * @returns resultado da subtração a - b
 */
export function subtract(a: number, b: number): number {
  return a - b;
}